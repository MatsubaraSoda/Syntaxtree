---
description: 与 .docs 冲突时以 .cursor/rules 为准；规范来源优先级
alwaysApply: true
---

# 文档与规范优先级

## 冲突处理

当 **`.docs/`** 下的说明、草稿、示例或脚本与 **`.cursor/rules/`** 中的项目规则**不一致**时，**一律以 `.cursor/rules/` 为准**。

`.docs/` 可能包含笔记、历史说明、实验代码或与当前实现不同步的内容，**不作为**架构约束或实现依据。

## Agent 行为

- 实现功能、修改代码、回答「项目应如何做」类问题时，以 **`app/` 源码** 与 **`.cursor/rules/`** 为权威。
- 可参考 `.docs/` 作背景阅读，但若与规则或代码矛盾，**忽略 `.docs` 中的矛盾部分**，不据此「纠正」规则或已约定的工作流。
